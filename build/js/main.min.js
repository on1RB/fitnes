/******/ (function(modules) { // webpackBootstrap
/******/ 	// The module cache
/******/ 	var installedModules = {};
/******/
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/
/******/ 		// Check if module is in cache
/******/ 		if(installedModules[moduleId]) {
/******/ 			return installedModules[moduleId].exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = installedModules[moduleId] = {
/******/ 			i: moduleId,
/******/ 			l: false,
/******/ 			exports: {}
/******/ 		};
/******/
/******/ 		// Execute the module function
/******/ 		modules[moduleId].call(module.exports, module, module.exports, __webpack_require__);
/******/
/******/ 		// Flag the module as loaded
/******/ 		module.l = true;
/******/
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/
/******/
/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = modules;
/******/
/******/ 	// expose the module cache
/******/ 	__webpack_require__.c = installedModules;
/******/
/******/ 	// define getter function for harmony exports
/******/ 	__webpack_require__.d = function(exports, name, getter) {
/******/ 		if(!__webpack_require__.o(exports, name)) {
/******/ 			Object.defineProperty(exports, name, { enumerable: true, get: getter });
/******/ 		}
/******/ 	};
/******/
/******/ 	// define __esModule on exports
/******/ 	__webpack_require__.r = function(exports) {
/******/ 		if(typeof Symbol !== 'undefined' && Symbol.toStringTag) {
/******/ 			Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
/******/ 		}
/******/ 		Object.defineProperty(exports, '__esModule', { value: true });
/******/ 	};
/******/
/******/ 	// create a fake namespace object
/******/ 	// mode & 1: value is a module id, require it
/******/ 	// mode & 2: merge all properties of value into the ns
/******/ 	// mode & 4: return value when already ns object
/******/ 	// mode & 8|1: behave like require
/******/ 	__webpack_require__.t = function(value, mode) {
/******/ 		if(mode & 1) value = __webpack_require__(value);
/******/ 		if(mode & 8) return value;
/******/ 		if((mode & 4) && typeof value === 'object' && value && value.__esModule) return value;
/******/ 		var ns = Object.create(null);
/******/ 		__webpack_require__.r(ns);
/******/ 		Object.defineProperty(ns, 'default', { enumerable: true, value: value });
/******/ 		if(mode & 2 && typeof value != 'string') for(var key in value) __webpack_require__.d(ns, key, function(key) { return value[key]; }.bind(null, key));
/******/ 		return ns;
/******/ 	};
/******/
/******/ 	// getDefaultExport function for compatibility with non-harmony modules
/******/ 	__webpack_require__.n = function(module) {
/******/ 		var getter = module && module.__esModule ?
/******/ 			function getDefault() { return module['default']; } :
/******/ 			function getModuleExports() { return module; };
/******/ 		__webpack_require__.d(getter, 'a', getter);
/******/ 		return getter;
/******/ 	};
/******/
/******/ 	// Object.prototype.hasOwnProperty.call
/******/ 	__webpack_require__.o = function(object, property) { return Object.prototype.hasOwnProperty.call(object, property); };
/******/
/******/ 	// __webpack_public_path__
/******/ 	__webpack_require__.p = "";
/******/
/******/
/******/ 	// Load entry module and return exports
/******/ 	return __webpack_require__(__webpack_require__.s = "./js/main.js");
/******/ })
/************************************************************************/
/******/ ({

/***/ "./js/main.js":
/*!********************!*\
  !*** ./js/main.js ***!
  \********************/
/*! no static exports found */
/***/ (function(module, exports) {

// ---------------------------------
window.addEventListener('DOMContentLoaded', function () {
  // Utils
  // ---------------------------------
  // Modules
  // ---------------------------------
  var videoPlayer = document.querySelector('.gym__video-player');
  var overlay = document.querySelector('.gym__video-overlay');
  var videoButton = document.querySelector('.gym__button');

  var removeClassNojs = function removeClassNojs(nameBlock) {
    var classNames = nameBlock.className.split(' ');
    nameBlock.classList.remove(classNames[0] + '--nojs');
  };

  if (overlay) {
    removeClassNojs(overlay);
  }

  if (videoPlayer) {
    removeClassNojs(videoPlayer);
  }

  if (videoButton) {
    removeClassNojs(videoButton);
    videoButton.addEventListener('click', function () {
      overlay.classList.add('gym__video-overlay--hidden');
      videoButton.classList.add('gym__button--hidden');
      videoPlayer.classList.remove('gym__video-player--hidden');
    });
  }

  var tabsNojs = document.querySelectorAll('.subscribe__inner');
  var tabsNojsList = Array.prototype.slice.call(tabsNojs);
  var subscribe = document.querySelectorAll('.subscribe__list');
  var subscribeList = Array.prototype.slice.call(subscribe);

  var removeTabsClassNojs = function removeTabsClassNojs(nameBlock) {
    var classNames = nameBlock.className.split(' ');

    if (nameBlock.classList.contains(classNames[0] + '--nojs')) {
      nameBlock.classList.remove(classNames[0] + '--nojs');
      nameBlock.classList.add(classNames[0] + '--hidden');
    }
  };

  if (tabsNojsList) {
    tabsNojsList.forEach(function (element) {
      removeTabsClassNojs(element);
    });
  }

  if (subscribeList) {
    subscribeList.forEach(function (element) {
      removeTabsClassNojs(element);
    });
  }

  var tabs = document.querySelectorAll('.subscribe__tabs-button');
  var tabsList = Array.prototype.slice.call(tabs);
  var firstMonth = document.querySelector('#first-month');
  var sixMonth = document.querySelector('#six-months');
  var twelveMonth = document.querySelector('#twelve-months');

  var addActiveClass = function addActiveClass(index) {
    tabsList.filter(function (_, i) {
      return i !== index;
    }).forEach(function (element) {
      element.classList.remove('subscribe__tabs-button--active');
    });
  };

  var switchesActiveElement = function switchesActiveElement(activeBlock, secondBlock, thirdBlock, currentIndex, index) {
    if (index === currentIndex) {
      activeBlock.classList.add('subscribe__list--active');
      secondBlock.classList.remove('subscribe__list--active');
      thirdBlock.classList.remove('subscribe__list--active');
      activeBlock.classList.remove('subscribe__list--hidden');
    } else {
      activeBlock.classList.remove('subscribe__list--active');
      activeBlock.classList.remove('subscribe__list--hidden');
    }
  };

  if (tabsList) {
    tabsList.forEach(function (element, index) {
      element.addEventListener('click', function () {
        element.classList.add('subscribe__tabs-button--active');
        addActiveClass(index);
        switchesActiveElement(firstMonth, sixMonth, twelveMonth, 0, index);
        switchesActiveElement(sixMonth, firstMonth, twelveMonth, 1, index);
        switchesActiveElement(twelveMonth, firstMonth, sixMonth, 2, index);
      });
    });
  }

  var trainersSliderContainer = document.querySelector('.trainers__slider');
  var trainersSlider = document.querySelector('.trainers__swiper');
  var trainersSliderContent = document.querySelector('.trainers__content');
  var trainersButton = document.querySelectorAll('.trainers__button');
  var trainersButtonList = Array.prototype.slice.call(trainersButton);

  if (trainersSliderContainer) {
    removeClassNojs(trainersSliderContainer);
  }

  if (trainersSlider) {
    removeClassNojs(trainersSlider);
  }

  if (trainersSliderContent) {
    removeClassNojs(trainersSliderContent);
  }

  if (trainersButtonList) {
    trainersButtonList.forEach(function (element) {
      removeClassNojs(element);
    });
  }

  var swiperTrainers = new Swiper('.swiper.trainers__swiper', {
    breakpoints: {
      320: {
        initialSlide: 2,
        slidesPerView: 1,
        spaceBetween: 30
      },
      768: {
        initialSlide: 2,
        slidesPerView: 2,
        spaceBetween: 30
      },
      1200: {
        initialSlide: 0,
        slidesPerView: 4,
        spaceBetween: 40
      }
    },
    direction: 'horizontal',
    loop: true,
    navigation: {
      nextEl: '.slider-button-next',
      prevEl: '.slider-button-prev'
    }
  });
  var swiperRewiews = new Swiper('.swiper.reviews__swiper', {
    slidesPerView: 1,
    direction: 'horizontal',
    loop: false,
    navigation: {
      nextEl: '.reviews__button--next',
      prevEl: '.reviews__button--prev'
    }
  });
  var rewiewsSlider = document.querySelector('.reviews__slider');
  var rewiewsContent = document.querySelector('.reviews__content');
  var rewiewsCard = document.querySelectorAll('.reviews__card');
  var rewiewsCardList = Array.prototype.slice.call(rewiewsCard);
  var rewiewsButton = document.querySelectorAll('.reviews__button');
  var rewiewsButtonList = Array.prototype.slice.call(rewiewsButton);

  if (rewiewsSlider) {
    removeClassNojs(rewiewsSlider);
  }

  if (rewiewsContent) {
    removeClassNojs(rewiewsContent);
  }

  if (rewiewsCardList) {
    rewiewsCardList.forEach(function (element) {
      removeClassNojs(element);
    });
  }

  if (rewiewsButtonList) {
    rewiewsButtonList.forEach(function (element) {
      removeClassNojs(element);
    });
  }

  window.addEventListener('load', function () {});
});

/***/ })

/******/ });
//# sourceMappingURL=main.min.js.map